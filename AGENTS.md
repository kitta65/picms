# Agent instructions

Use English for GitHub writes and Git-tracked content, including commits, code, comments, documentation, configuration, and UI strings.
For all other output, such as chat responses and untracked plan files, use the requester's language unless they request otherwise.
