{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		// Environment setup & latest features
		"lib": ["ESNext", "DOM"],
		"types": ["bun", "react"],

		"paths": {
			"@/*": ["./src/*"]
		}
	},

	"exclude": ["dist", "node_modules"]
}
