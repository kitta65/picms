export type Work = {
	id: string;
	thumbnail: string;
	title: string;
	tags: string[];
	createdAt: Date;
	updatedAt: Date;
};

export type Version = {
	id: string;
	thumbnail: string;
	createdAt: Date;
};
