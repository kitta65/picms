{
	"name": "picms-main",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build --production --outfile=dist/picms --compile --define NODE_ENV='\"production\"' src/index.ts",
		"dev": "bun --inspect=0.0.0.0:${PICMS_PORT_INSPECT}/scripts-dev --hot src/index.ts",
		"lint": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"picms-server": "workspace:*",
		"picms-shared": "workspace:*",
		"picms-web": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
