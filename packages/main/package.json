{
	"name": "picms-main",
	"version": "0.1.0",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build --production --outfile=dist/picms --compile index.ts",
		"lint": "tsc --noEmit",
		"dev": "bun --inspect=0.0.0.0:6499/scripts-dev --hot index.ts",
		"test": "bun test"
	},
	"dependencies": {
		"picms-common": "workspace:*",
		"picms-server": "workspace:*",
		"picms-web": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
